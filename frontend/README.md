# EduPath Frontend Application Shell

React 18 + Vite + Tailwind CSS application shell for EduPath.

## Local Setup

```bash
# Install dependencies
npm install

# Start Vite dev server
npm run dev
```

App runs on `http://localhost:5173`. Proxies `/api/v1` to `http://localhost:8000`.
